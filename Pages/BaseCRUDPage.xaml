<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="test2.Pages.BaseCRUDPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900" Title="Base CRUD Page">
    <Page.Resources>
 <Style x:Key="ActionButtonStyle" TargetType="Button">
  <Setter Property="Padding" Value="15,8"/>
        <Setter Property="Margin" Value="5,0"/>
  <Setter Property="Foreground" Value="White"/>
   <Setter Property="BorderThickness" Value="0"/>
   <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Page.Resources>
    <Grid Margin="20">
  <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
     <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
   <TextBlock x:Name="txtTitle" Grid.Row="0" Text="TIEU DE TRANG" FontSize="20" FontWeight="Bold" Foreground="#2c3e50" Margin="0,0,0,20"/>
        <WrapPanel Grid.Row="1" Margin="0,0,0,15">
         <Button Content="Them moi" Style="{StaticResource ActionButtonStyle}" Background="#27ae60" Click="BtnThem_Click"/>
<Button Content="Sua" Style="{StaticResource ActionButtonStyle}" Background="#3498db" Click="BtnSua_Click"/>
<Button Content="Xoa" Style="{StaticResource ActionButtonStyle}" Background="#e74c3c" Click="BtnXoa_Click"/>
     <Button Content="Lam moi" Style="{StaticResource ActionButtonStyle}" Background="#95a5a6" Click="BtnLamMoi_Click"/>
        </WrapPanel>
        <DataGrid x:Name="dgData" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" Background="White" CanUserAddRows="False"/>
    </Grid>
</Page>
