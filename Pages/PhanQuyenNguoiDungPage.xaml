<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="test2.Pages.PhanQuyenNguoiDungPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900" Title="Phan quyen cho nguoi dung">
  <Grid Margin="20">
        <Grid.RowDefinitions>
<RowDefinition Height="Auto"/>
     <RowDefinition Height="Auto"/>
     <RowDefinition Height="*"/>
 </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="PHAN QUYEN CHO NGUOI DUNG" FontSize="20" FontWeight="Bold" Foreground="#2c3e50" Margin="0,0,0,20"/>
      <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="20" Margin="0,0,0,15">
   <WrapPanel>
       <StackPanel Margin="0,0,20,10" Width="250">
 <TextBlock Text="Chon nguoi dung:" FontWeight="SemiBold" Margin="0,0,0,5"/>
 <ComboBox x:Name="cboNguoiDung" Padding="8"/>
      </StackPanel>
     <Button Content="Luu phan quyen" Padding="20,10" Background="#27ae60" Foreground="White" VerticalAlignment="Bottom" Margin="0,0,0,10" Click="BtnLuu_Click"/>
    </WrapPanel>
        </Border>
<DataGrid x:Name="dgQuyen" Grid.Row="2" AutoGenerateColumns="False" Background="White" CanUserAddRows="False">
          <DataGrid.Columns>
 <DataGridTextColumn Header="Ma quyen" Binding="{Binding MaQuyen}" Width="100" IsReadOnly="True"/>
 <DataGridTextColumn Header="Ten quyen" Binding="{Binding TenQuyen}" Width="*" IsReadOnly="True"/>
     <DataGridTextColumn Header="Mo ta" Binding="{Binding MoTa}" Width="250" IsReadOnly="True"/>
  <DataGridCheckBoxColumn Header="Duoc phep" Binding="{Binding DuocPhep}" Width="80"/>
            </DataGrid.Columns>
    </DataGrid>
    </Grid>
</Page>
