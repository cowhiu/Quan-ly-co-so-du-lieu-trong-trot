<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="test2.Pages.DanhMucHuyenPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 mc:Ignorable="d"
 d:DesignHeight="600" d:DesignWidth="900"
   Title="Quan ly danh muc Huyen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
  <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
  <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="QUAN LY DANH MUC HUYEN" FontSize="20" FontWeight="Bold" Foreground="#2c3e50" Margin="0,0,0,20"/>
        <WrapPanel Grid.Row="1" Margin="0,0,0,15">
     <Button Content="Them moi" Padding="15,8" Margin="5,0" Background="#27ae60" Foreground="White" Click="BtnThem_Click"/>
            <Button Content="Sua" Padding="15,8" Margin="5,0" Background="#3498db" Foreground="White" Click="BtnSua_Click"/>
            <Button Content="Xoa" Padding="15,8" Margin="5,0" Background="#e74c3c" Foreground="White" Click="BtnXoa_Click"/>
            <Button Content="Lam moi" Padding="15,8" Margin="5,0" Background="#95a5a6" Foreground="White" Click="BtnLamMoi_Click"/>
        </WrapPanel>
        <DataGrid x:Name="dgHuyen" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" Background="White" CanUserAddRows="False">
            <DataGrid.Columns>
 <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
     <DataGridTextColumn Header="Ma Huyen" Binding="{Binding MaHuyen}" Width="100"/>
     <DataGridTextColumn Header="Ten Huyen" Binding="{Binding TenHuyen}" Width="*"/>
         <DataGridTextColumn Header="Tinh" Binding="{Binding TenTinh}" Width="150"/>
         <DataGridCheckBoxColumn Header="Trang thai" Binding="{Binding TrangThai}" Width="80"/>
    </DataGrid.Columns>
  </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
    <Button Content="Trang truoc" Padding="10,5" Margin="5,0" Click="BtnTrangTruoc_Click"/>
            <TextBlock x:Name="txtTrang" Text="Trang 1/1" VerticalAlignment="Center" Margin="10,0"/>
         <Button Content="Trang sau" Padding="10,5" Margin="5,0" Click="BtnTrangSau_Click"/>
        </StackPanel>
    </Grid>
</Page>
